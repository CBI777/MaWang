using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ShopRelated : MonoBehaviour
{
    /*shop의 경우에는 Dictionary를 가지고 있을 예정
     * <int, Artifact>의 형태로, 각 int를 통해서 그에 해당되는 artifact를 가져오는 형태.
     * 즉, SaveBase의 StageVar에서 저장해놓은
     * int값을 통해서 그에 해당되는 artifact n개를 보여주는 것이다.
     */
}
